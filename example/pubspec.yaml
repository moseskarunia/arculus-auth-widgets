name: example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.8.0 <3.0.0"
  flutter: 1.22.5

dependencies:
  flutter:
    sdk: flutter
  # arculus_auth_widgets:
  #     path: ../
  flutter_svg: 0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/